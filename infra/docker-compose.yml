# Standardising Compose Configuration For Modern Docker

services:
  postgres:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
    ports:
      - "5433:5432"
    volumes:
      - ./postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 3s
      timeout: 3s
      retries: 5

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"

  api-gateway:
    image: nginx:alpine
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      # Remapping Host Port To 8088 Due To Local Conflict On Port 8080
      - "8088:8080"
    depends_on:
      - account-service
      - transaction-service
      - ledger-service
      - fx-service
      - payroll-service
      - admin-service

  account-service:
    build:
      context: ../
      dockerfile: Dockerfile
    command: >
      sh -c "npx prisma db push --schema=services/account-service/prisma/schema.prisma --url $$DATABASE_URL --accept-data-loss && npx prisma generate --schema=services/account-service/prisma/schema.prisma && npm run dev -w services/account-service"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/account_service_db
      - MASTER_ENCRYPTION_KEY=d5fb72b325bbba1be66c3f883e86d53bc2fc86a7f0d6ac0bd88cc88072c5c717
    ports:
      - "3001:3001"
    volumes:
      - ../:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy

  ledger-service:
    build:
      context: ../
      dockerfile: Dockerfile
    command: >
      sh -c "npx prisma db push --schema=services/ledger-service/prisma/schema.prisma --url $$DATABASE_URL --accept-data-loss && npx prisma generate --schema=services/ledger-service/prisma/schema.prisma && npm run dev -w services/ledger-service"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/ledger_service_db
      - MASTER_ENCRYPTION_KEY=d5fb72b325bbba1be66c3f883e86d53bc2fc86a7f0d6ac0bd88cc88072c5c717
    ports:
      - "3003:3003"
    volumes:
      - ../:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy

  transaction-service:
    build:
      context: ../
      dockerfile: Dockerfile
    command: >
      sh -c "npx prisma db push --schema=services/transaction-service/prisma/schema.prisma --url $$DATABASE_URL --accept-data-loss && npx prisma generate --schema=services/transaction-service/prisma/schema.prisma && npm run dev -w services/transaction-service"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/transaction_service_db
      - FX_SERVICE_URL=http://fx-service:3006
      - LEDGER_SERVICE_URL=http://ledger-service:3003
    ports:
      - "3002:3002"
    volumes:
      - ../:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy

  fx-service:
    build:
      context: ../
      dockerfile: Dockerfile
    command: >
      sh -c "npx prisma db push --schema=services/fx-service/prisma/schema.prisma --url $$DATABASE_URL --accept-data-loss && npx prisma generate --schema=services/fx-service/prisma/schema.prisma && npm run dev -w services/fx-service"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/fx_service_db
    ports:
      - "3006:3006"
    volumes:
      - ../:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy

  payroll-service:
    build:
      context: ../
      dockerfile: Dockerfile
    command: >
      sh -c "npx prisma db push --schema=services/payroll-service/prisma/schema.prisma --url $$DATABASE_URL --accept-data-loss && npx prisma generate --schema=services/payroll-service/prisma/schema.prisma && npm run dev -w services/payroll-service"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@postgres:5432/payroll_service_db
      - REDIS_URL=redis://redis:6379
      # Standardising Redis Connection Parameters For Reliable BullMQ Worker Integration
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - TRANSACTION_SERVICE_URL=http://transaction-service:3002
    ports:
      - "3005:3005"
    volumes:
      - ../:/app
      - /app/node_modules
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  admin-service:
    build:
      context: ../
      dockerfile: Dockerfile
    command: >
      sh -c "npm run dev -w services/admin-service"
    environment:
      - ACCOUNT_SERVICE_URL=http://account-service:3001
      - LEDGER_SERVICE_URL=http://ledger-service:3003
      - TRANSACTION_SERVICE_URL=http://transaction-service:3002
      - PORT=3007
    ports:
      - "3007:3007"
    volumes:
      - ../:/app
      - /app/node_modules

  prometheus:
    image: prom/prometheus:v2.45.0
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro

  grafana:
    image: grafana/grafana:10.0.3
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - ./grafana/dashboards:/var/lib/grafana/dashboards:ro
    depends_on:
      - prometheus

  jaeger:
    image: jaegertracing/all-in-one:1.47
    ports:
      - "16686:16686" # Web UI
      - "4317:4317"   # OTLP gRPC
      - "4318:4318"   # OTLP HTTP

volumes:
  postgres_data:
