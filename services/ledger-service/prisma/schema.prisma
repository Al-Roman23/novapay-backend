generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider = "postgresql"
}

model LedgerAccount {
  id                String   @id @default(uuid())
  encryptedWalletId String
  walletIdIv        String
  walletIdDek       String
  walletIdDekIv     String
  
  // A Searchable Hashed Identity For Secure Inter-Service Communication
  walletIdHash      String?  @unique

  currency          String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  entries   LedgerEntry[]
}

model LedgerEntry {
  id              String   @id @default(uuid())
  ledgerAccountId String

  type            EntryType
  amount          Decimal

  rate            Decimal?  // Record Locked FX Rate For Audit
  fromAmount      Decimal?  // Original Amount Sent
  toAmount        Decimal?  // Target Amount After Conversion

  transactionId   String

  previousHash    String?
  hash            String    @unique

  createdAt       DateTime @default(now())

  ledgerAccount   LedgerAccount @relation(fields: [ledgerAccountId], references: [id])

  @@index([transactionId])
  @@index([hash])
}

enum EntryType {
  DEBIT
  CREDIT
}
