generator client {
  provider = "prisma-client-js"
  output   = "../generated/client"
}

datasource db {
  provider = "postgresql"
}

model Transaction {
  id             String   @id @default(uuid())
  fromWalletId   String
  toWalletId     String
  amount         Decimal
  currency       String
  status         TransactionStatus @default(PENDING)

  toCurrency     String?
  quoteId        String?
  lockedRate     Decimal?  // Store Locked Rate At Creation
  targetAmount   Decimal?  // Amount Receiver Gets

  idempotencyKey String   @unique
  idempotencyExpiresAt DateTime
  requestHash    String

  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt

  @@index([fromWalletId])
  @@index([toWalletId])
  @@index([createdAt])
}

enum TransactionStatus {
  PENDING
  PROCESSING
  COMPLETED
  FAILED
}
